// Copyright 2020 Prescryptive Health, Inc.

import { ViewStyle } from 'react-native';

export interface ISurveySingleSelectStyles {
  pickerContainerTextStyle: ViewStyle;
}

const pickerContainerTextStyle: ViewStyle = {
  width: '100%',
};

export const surveySingleSelectStyles: ISurveySingleSelectStyles = {
  pickerContainerTextStyle,
};
