// Copyright 2021 Prescryptive Health, Inc.

import { DistancePickerContent } from './distance-picker.content';

describe('DistancePickerContent', () => {
  it('has expected content', () => {
    expect(DistancePickerContent.miles).toEqual('mi.');
  });
});
